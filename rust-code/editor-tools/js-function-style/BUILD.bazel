load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "js-function-style",
    srcs = ["main.rs"],
    deps = [
        "@crates//:tree-sitter",
        "@crates//:tree-sitter-typescript",
    ],
)
